server.port=8080
# Spring Boot 3.2+ requires spring.data.redis (spring.redis.* is silently ignored)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
cache.home.ttl-minutes=5
cache.home.local-ttl-seconds=30
cache.lock.ttl-seconds=5
cache.local.ttl-ms=30000